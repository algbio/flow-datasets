# complex32 cyclic graphs

This directory contains generated cyclic graph outputs for the `complex32` dataset.

For dataset description, source genomes, and generation parameters see:
`../../datasets/complex32/README.md`

Separated into `perfect-weights` and `imperfect-weights` directories.
